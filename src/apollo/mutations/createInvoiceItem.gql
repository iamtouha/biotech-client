mutation createInvoiceItem($input: createInvoiceItemInput!) {
  createInvoiceItem(input: $input) {
    invoiceItem {
      id
      units
      type
      rate
      pack {
        product {
          id
          name
        }
        size
      }
    }
  }
}
